# the wandb run name comes from this file's name -- rename the file to rename the run
model: logistic_regression
# optional features to switch on -- valid names are the keys of OPTIONAL_FEATURES in preprocessing.py
features: [male_x_3rdclass]
params:
  max_iter: 1000
